<Page
    x:Class="ReadNow.Views.AddItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReadNow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
        </Grid>

        <TextBlock TextWrapping="Wrap" Grid.Row="0" Margin="0,15,0,0" x:Name="textBoxUrl" FontSize="16" HorizontalAlignment="Center" x:FieldModifier="public"></TextBlock>

        <ProgressRing Grid.Row="1" x:Name="progressBar" x:FieldModifier="public" IsActive="True" />

        <TextBlock Grid.Row="2" x:Name="textboxAdded" Visibility="Collapsed" HorizontalAlignment="Center" FontSize="24" VerticalAlignment="Center" Text="The url has been successfully added to Pocket">
        </TextBlock>
    </Grid>
</Page>